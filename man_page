.\"Manpage for AirBnB Console.
.TH _AirBnB_clone Console
.SH NAME
AirBnB Conole \- Manage the AirBnB clone database with user's defined commands.
.SH SYNOPSIS
.B ./console.py
.SH DESCRIPTION
.TP
.B ./CONSOLE.PY:
Open a user console where input can be entered and evaluated by the engine behind the scene.
.TP
Command defined for the user to communicate with the engine via the console.
.TP
.B quit \- exit the console.
.TP
.B create \- Creates a new instance of BaseModel, saves it (to the JSON file and prints the id.
.TP
.b Show \- prints the string representation of an instance based on the class 'name' and `id`.
.TP
.B destroy \- Deletes an instance based on the class name and id (save the change into the JSON file)
.TP
.B all \- prints all string representation of all instances based or not on the class name.
.TP
.B update \- Updates an instance based on the class name and id by adding or updating attribute (save the change into the JSON file)
              Usage: update <class name> <id> <attribute name> "attribute value>"

.SH EXAMPLES
To show all elements in the database, the user will enter the command all into the console.
.BR ./console.py
command:
.pp
.nf
RS
all()
.RE
.fi
.pp
.SH AUTHOR
JOSEPH NATHAN
